# Copy to .npmrc in this repo (for publishing) OR in the consuming project (for installing).
# The token is a Gitea access token with package read (install) / write (publish) scope.
# Do NOT commit the real token — keep .npmrc gitignored and pass GITEA_TOKEN via env.

@blackpoi55:registry=https://gitea.telecorpthailand.com/api/packages/blackpoi55/npm/
//gitea.telecorpthailand.com/api/packages/blackpoi55/npm/:_authToken=72d7fa391171d4d38d45b35b5df8162cc4c91bb8
